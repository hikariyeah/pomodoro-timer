# 🍅 番茄钟使用指南

> 一个不用安装、不用联网、双击就能用的专注计时器。这份指南给完全没用过的小白看，照着做就行。

---

## 一、什么是番茄钟？

番茄工作法很简单：

1. **专注 25 分钟**，只做一件事，不看手机。
2. 时间到，**休息 5 分钟**，喝水、走动一下。
3. 这样「专注 + 休息」算一轮。**每完成 4 轮**，就给自己一次**长休息（15 分钟）**。

这个小工具会帮你自动计时、自动切换、自动响铃，还会**记录你每天专注了多少**。

---

## 二、怎么打开？

- **双击** `pomodoro-timer.html` 这个文件，它会自动用浏览器打开。
- 不需要联网，不需要安装任何东西。
- 想换个地方用？把这个文件复制走就行，它是「一个文件搞定一切」的。

> 💡 这份说明文档 `番茄钟使用指南.md` 和计时器放在**同一个文件夹**里，方便随时翻看。

---

## 三、主界面怎么用？

打开后你会看到一个大大的圆环和时间，下面三步就能上手：

| 你想做的事 | 怎么操作 |
|---|---|
| 开始计时 | 点中间的 **开始** 按钮（或按一下**空格键**） |
| 暂停 | 再点一次（变成 **暂停**），或再按空格 |
| 重新开始这一段 | 点左边的 **↺**（或按 **R** 键） |
| 跳到下一段 | 点右边的 **⏭** |

**顶部三个标签**可以手动切换模式：

- **专注** —— 干活的时间（默认 25 分钟）
- **短休息** —— 小憩（默认 5 分钟）
- **长休息** —— 大休息（默认 15 分钟）

时间走完会**自动响铃**并切换到下一段，你不用管。背景颜色也会跟着模式变化，一眼就知道现在该专注还是该休息。

底部左下角的 **🍅 今日 X 个** 会告诉你今天已经完成了几个番茄。

---

## 四、看自己的专注记录（历史 📊）

点底部的 **历史 📊** 按钮，会展开你的记录：

- 顶部一行统计：**完成了几个番茄、累计专注多少分钟、中断了几次**。
- 下面是每一条记录，按「今天 / 昨天 / 日期」分组：
  - 🍅 **专注 · 完成** —— 完整做完的一个番茄
  - ☕ / 🌙 **休息 · 完成** —— 完成的短休息 / 长休息
  - ⛔ **中断** —— 中途放弃的（会用删除线标出，并显示你实际坚持了多久）

> 这些记录会**自动保存在浏览器里**，关掉再打开还在，不用担心丢。
>
> 想清空？点历史面板右上角的 **清空** 按钮（会先问你一次，确认才删）。

---

## 五、设置 ⚙（两部分）

点底部 **设置 ⚙**，里面分两块：

### 1️⃣ 时长设置

用 **−／＋** 按钮调整：

- **专注（分钟）**：默认 25
- **短休息（分钟）**：默认 5
- **长休息（分钟）**：默认 15
- **几轮后长休息**：默认 4（即每 4 个番茄后来一次长休息）

调完会自动记住，下次打开还是你设的值。

### 2️⃣ 数据恢复

这一块是用来**备份和搬运你的记录**的，有三个按钮：

#### ⬇ 导出
点一下，会下载一个 `pomodoro-history.json` 文件（里面是你所有的记录 + 设置）。

👉 **建议把它放到和计时器同一个文件夹里**，当作备份。

#### ⬆ 导入
换了电脑、或想恢复之前的备份时，点「导入」选中那个 `.json` 文件，记录就回来了。

#### 🔗 连接文件（自动同步，推荐 Chrome / Edge 用户）
点一下，选择（或新建）一个 `pomodoro-history.json`，**之后每完成或中断一个番茄都会自动写进这个文件**，不用再手动导出。

- 建议把这个文件就建在**计时器所在的文件夹**里，这样数据和程序永远在一起。
- 下次重新打开网页时，按钮会显示「🔗 重新连接」，点一下授权即可继续自动同步（这是浏览器的安全规定，省不掉）。
- 如果你用的是 **Safari 或 Firefox**，这个按钮会显示「不支持」——没关系，用上面的「导出 / 导入」一样能备份。

---

## 六、怎么确认记录真的存下来了？（30 秒自测）

1. 进「设置 ⚙ → 时长设置」，把**专注**调到 **1 分钟**。
2. 回到主界面点**开始**，等它走完（会响铃）。
3. 点「历史 📊」，应该能看到一条 🍅 **完成** 记录。
4. 再开始一次，中途点 **↺** 重置 —— 会出现一条 ⛔ **中断** 记录。
5. **完全关掉浏览器再重新打开** —— 记录还在，就说明保存成功了 ✅。
6. （进阶）如果你用了「连接文件」，可以直接用记事本／访达打开那个 `.json` 文件，里面就是你真实的记录，眼见为实。

> 别忘了把专注时长**调回 25 分钟**再正式使用哦。

---

## 七、键盘快捷键

| 按键 | 作用 |
|---|---|
| **空格** | 开始 / 暂停 |
| **R** | 重置当前这一段 |

---

## 八、常见疑问

**Q：数据存在哪里？会上传到网上吗？**
A：完全不会。默认存在你这台电脑的浏览器本地（localStorage），或者你自己选的那个 `.json` 文件里。没有任何联网上传。

**Q：换浏览器记录会丢吗？**
A：浏览器里的记录是「跟着浏览器走」的。换浏览器或换电脑前，先「导出」备份，到新地方再「导入」即可。

**Q：「连接文件」点了没反应？**
A：可能是浏览器不支持（Safari/Firefox），或当前打开方式受限。用「导出 / 导入」一定可行。

**Q：响铃声音是哪来的？**
A：程序临时合成的「叮」声，不需要任何音频文件，所以离线也能响。

---

祝你专注顺利，今天也收获满满的 🍅！
